name: Generate Diziyou M3U

on:
  schedule:
    # Her 36 saatte bir çalıştır (Cron formatında)
    - cron: '0 */36 * * *'
  workflow_dispatch:  # Manuel çalıştırma için
  push:
    branches: [ main ]

jobs:
  generate-m3u:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🛎️ Repository'yi kontrol et
      uses: actions/checkout@v3
      
    - name: 🐍 Python kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Bağımlılıkları yükle
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4
        
    - name: 🚀 M3U oluşturucuyu çalıştır
      run: |
        python diziyou_m3u.py
        
    - name: 📝 Değişiklikleri kontrol et
      id: git-check
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # M3U dosyasında değişiklik var mı kontrol et
        if git diff --quiet diziyou.m3u; then
          echo "changes=false" >> $GITHUB_OUTPUT
          echo "ℹ M3U dosyasında değişiklik yok"
        else
          echo "changes=true" >> $GITHUB_OUTPUT
          echo "✅ M3U dosyası güncellendi"
        fi
        
    - name: 💾 Değişiklikleri kaydet
      if: steps.git-check.outputs.changes == 'true'
      run: |
        git add diziyou.m3u
        git commit -m "🤖 M3U dosyası otomatik güncellendi [$(date +'%Y-%m-%d %H:%M')]"
        git push
